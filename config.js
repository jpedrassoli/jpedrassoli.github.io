// Configurações do app. Edite só este arquivo.
window.APP_CONFIG = {
  // URL do Apps Script (termina em /exec), entre as aspas.
  // Vazio = modo local (os pontos ficam só neste navegador).
  SCRIPT_URL: "",

  // Opcional: link da Planilha Google, para o botão "Abrir no Google Planilhas".
  SHEET_URL: "",

  // De quanto em quanto tempo (em milissegundos) buscar os pontos dos colegas.
  POLL_MS: 5000,

  // true  = mostra o painel "Modo de teste" (gerar pontos aleatórios).
  // false = versão final, sem o painel de teste.
  TESTER: false
};
